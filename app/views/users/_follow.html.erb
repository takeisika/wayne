<%if @current_user%>
<%if @current_user.id!=user.id%>
          <% @follow=Follow.find_by(following_id: @current_user.id,followed_id: user.id) %>
             <%if @follow.nil?%>
                <p class="not_follow_button"><%=link_to("follow","/follows/#{user.id}/create",method: :post,remote: true)%></p>
             <%else%>
                <p class="follow_button"><%=link_to("following","/follows/#{user.id}/destroy",method: :post,remote: true)%></p>
             <%end%>
<%end%>
<%else%>
  <p class="not_follow_button"><%=link_to("follow","/follows/#{user.id}/create",{method: :post})%></p>
<%end%>